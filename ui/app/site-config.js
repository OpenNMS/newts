

(function() {
    'use strict';

    angular
        .module('newts.siteConfig', [])
        .constant('siteConfig', {
            'restHost': 'localhost',
            'restPort': 8080
        });
    
})();
